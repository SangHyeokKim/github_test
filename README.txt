git hub test를 위한 파일입니다.
add
clone에서 내용추가

mac os에서 VScode를 가지고 git hub를 테스트합니다.

stage를 VSCode에서 해서 그런건가? Smartgit에서 파일의 변화가 뜨질 않는다.
이번에는 VSCode에서 stage를 하지 않고 진행해보자.

VSCode에서 stage를 하지 않았을 때 똑바로 진행이 되는것을 확인했고.
이번에는 VSCode에서 stage를 진행하고 해보자.

add brance from MacBook

--------------------------------------
Branch를 만든 후에 merge를 테스트 한다.
merge를 할 때에는 branch를 새로 만들 것인지 아니면 기존에 있던 branch에 합칠 것인지 선택을 할 수 있는데
이 둘 모두를 테스트 해 본다 실제 사용할 것은 아무래도 기존 것으로 합치는 형태가 될 것으로 생각이 된다.